name: Databricks CI

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate repository
        run: |
          echo "Repository checkout successful"
          echo "Databricks pipeline CI started"
